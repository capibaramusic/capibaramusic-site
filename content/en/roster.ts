import type { RosterContent } from "../types";

const roster = {
  heading: "Artists",
  openLabel: "Open information for",
  closeLabel: "Close information for",
} satisfies RosterContent;

export default roster;
