import type { RosterContent } from "../types";

const roster = {
  heading: "Artistas",
  openLabel: "Abrir información de",
  closeLabel: "Cerrar información de",
} satisfies RosterContent;

export default roster;
